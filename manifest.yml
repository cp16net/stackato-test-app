applications:
- name: hod-test-app
  buildpack: https://github.com/cloudfoundry/go-buildpack
  memory: 32M
  stackato:
    env:
      GOVERSION:
        default: 1.6.2
      GO15VENDOREXPERIMENT: 0
    ignores:
    - .git
    # hooks:
      # post-staging:
      # setting up in filesystem
      # - sh mysql-setup.sh
  services:
  - cp16net-mysql
  - cp16net-hod
  - cp16net-googleapi
